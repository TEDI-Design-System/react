import * as React from 'react';

/**
 * ShowAt — from @tedi-design-system/react@0.0.0-semantic-version (./src/tedi/components/layout/show-at/show-at.stories.tsx).
 */
export interface ShowAtProps {
  children: React.ReactNode;
  xs?: boolean;
  sm?: boolean;
  md?: boolean;
  lg?: boolean;
  xl?: boolean;
  xxl?: boolean;
}

export declare const ShowAt: React.ComponentType<ShowAtProps>;
