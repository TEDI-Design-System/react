import * as React from 'react';

/**
 * HideAt — from @tedi-design-system/react@0.0.0-semantic-version (./src/tedi/components/layout/hide-at/hide-at.stories.tsx).
 */
export interface HideAtProps {
  children: React.ReactNode;
  xs?: boolean;
  sm?: boolean;
  md?: boolean;
  lg?: boolean;
  xl?: boolean;
  xxl?: boolean;
}

export declare const HideAt: React.ComponentType<HideAtProps>;
