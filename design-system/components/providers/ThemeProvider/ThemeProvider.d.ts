import * as React from 'react';

/**
 * ThemeProvider — from @tedi-design-system/react@0.0.0-semantic-version (./src/tedi/providers/theme-provider/theme-provider.stories.tsx).
 */
export interface ThemeProviderProps {
  theme?: string;
  children: React.ReactNode;
}

export declare const ThemeProvider: React.ComponentType<ThemeProviderProps>;
